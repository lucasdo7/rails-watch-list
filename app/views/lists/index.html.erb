<div class="container">
  <h1>all lists here !!</h1> <br>


  <%= link_to "Create a new List", new_list_path, class: 'btn btn-success' %> <hr>

  <% @lists.each do |l| %>
    <li><strong><%= l.name.capitalize %></strong> <%= link_to "Details", list_path(l) %> </li> <hr>
  <% end %>
</div>
